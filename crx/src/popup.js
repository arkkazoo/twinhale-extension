document.addEventListener('DOMContentLoaded', () => {
  const goodButton = document.getElementById('goodButton')
  goodButton.addEventListener('click', () => {
    // Goodボタンがクリックされた時の処理を記述する
    alert('You clicked the Good button!')
  })
})
